pipeline {
	agent any
	stages {
		stage('Init') {
			steps {
				echo 'Hi, this is sudhansu from odisa'
				echo 'We are Starting the T3 Cloud'
			}
		}
		stage('Build') {
			steps {
				echo 'Building Sample Maven Project'
			}
		}
		stage('Deploy') {
			stpes {
				echo "Deploying in staging area"
			}
		}
	}
}

